extends ../layout

block content

  h1 Sections
    a.btn.btn-primary.pull-right(href='/admin/section/new') New Section

  .table-responsive
    table.table.table-striped
      thead
        tr
          th Edit
          th Headline
          th Slug
          th Technologies
          th Live
          th Published
          th Created
          th Delete
      tbody
        for item in items
          tr
            td
              a.btn.btn-primary(href="/admin/section/edit/" + item.slug) Edit
            td
              a(href="/section/" + item.slug + "?preview")=item.headline
            td=item.slug
            td=item.tech
            td=item.published.long
            td
              if item.isLive
                span.label.label-success Yes
              else
                span.label.label-danger No
            td=item.created
            td
              a.btn.btn-danger(href="/admin/section/delete/" + item._id) Delete

